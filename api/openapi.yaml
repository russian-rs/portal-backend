openapi: 3.0.1
info:
  title: volunteer-portal-api
  version: 1.34.0
  contact:
    email: dev@russian.rs
    url: https://russian.rs

servers:
  - url: https://REDACTED_HOST/api
    description: Test Environment
  - url: https://portal.russian.rs/api
    description: Prod Environment

paths:
  /user/account:
    get:
      operationId: getCurrentAccount
      tags:
        - user
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      operationId: createUser
      tags:
        - user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserCreateRequest"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
  /user/account/{id}/program/{code}:
    patch:
      operationId: setProgram
      tags:
        - user
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            nullable: false
        - in: path
          name: code
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
  /user/account/{id}/project/{code}:
    patch:
      operationId: setProject
      tags:
        - user
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            nullable: false
        - in: path
          name: code
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
  /user/account/{id}/deactivate:
    post:
      operationId: deactivateAccount
      tags:
        - user
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            nullable: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
  /user/account/{id}/activate:
    post:
      operationId: activateAccount
      tags:
        - user
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            nullable: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
  /user/account/{id}/contracts:
    post:
      operationId: updateContracts
      description: Обновить список договоров волонтера
      tags:
        - user
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            nullable: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "#/components/schemas/ContractDto"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
  /user/account/{id}/residence-permits:
    put:
      operationId: updateResidencePermits
      description: Обновить список ВНЖ волонтера
      tags:
        - user
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "#/components/schemas/ResidencePermitDto"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
  /user/account/{id}/residence-permits/{permitId}:
    delete:
      operationId: deleteResidencePermit
      description: Удалить ВНЖ волонтера (только для администраторов)
      tags: [ user ]
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - { in: path, name: permitId, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
  /user/info/{login}:
    get:
      operationId: getInfo
      tags:
        - user
      parameters:
        - name: login
          in: path
          description: User login
          required: true
          schema:
            type: string
            minLength: 3
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
        "500":
          $ref: "#/components/responses/ServerError"

    patch:
      operationId: updateInfo
      tags:
        - user
      parameters:
        - name: login
          in: path
          description: User login
          required: true
          schema:
            type: string
            minLength: 3
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserInfoUpdateRequest"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
        "500":
          $ref: "#/components/responses/ServerError"

  /user/search:
    post:
      operationId: searchUsers
      tags:
        - user
      parameters:
        - name: searchQuery
          in: query
          required: true
          schema:
            type: string
        - name: pageRequest
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/PageRequest"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserSearchFilter"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserPageResponse"
  /user/logout:
    post:
      operationId: logout
      tags:
        - user
      parameters:
        - name: all
          in: query
          schema:
            type: boolean
            default: false
          description: Logout from all devices
          required: false
      responses:
        "200":
          description: Successfully logged out
        "500":
          $ref: "#/components/responses/ServerError"
  /user/avatar:
    post:
      operationId: setAvatar
      tags:
        - user
      parameters:
        - name: avatarId
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoDto"
  /user/resolve:
    post:
      operationId: resolveUsers
      tags:
        - user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              default: [ ]
              items:
                type: string
                maxLength: 100
                minLength: 1
                nullable: false
                description: username
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                default: [ ]
                items:
                  $ref: "#/components/schemas/UserInfoDto"

  /files/upload:
    post:
      operationId: uploadFile
      tags:
        - files
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  nullable: false
      responses:
        "200":
          description: Successfully uploaded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileInfoDto"
  /files/{id}:
    get:
      operationId: getFileInfo
      tags:
        - files
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            nullable: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileInfoDto"
        "400":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /report:
    post:
      operationId: createReport
      tags:
        - report
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportDto"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportDto"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      operationId: updateReport
      tags:
        - report
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportDto"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportDto"
  /report/{id}:
    get:
      operationId: getReport
      tags:
        - report
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportDto"
        "404":
          $ref: "#/components/responses/ServerError"
    delete:
      operationId: deleteReport
      tags:
        - report
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Success
        "404":
          $ref: "#/components/responses/ServerError"
  /report/{id}/note:
    post:
      operationId: addNote
      tags:
        - report
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NoteDto"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteDto"
  /report/{id}/status:
    post:
      operationId: changeStatus
      tags:
        - report
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangeReportStatusRequest"
      responses:
        "200":
          description: Success
  /reports:
    post:
      operationId: getReports
      tags:
        - report
      parameters:
        - name: pageRequest
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/PageRequest"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportFilter"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportPageResponse"
  /reports/heat-map:
    get:
      operationId: getCurrentUserHeatMapDeprecated
      description: (deprecated) Тепловая карта отчетов текущего пользователя
      tags:
        - report-heat-map
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VolunteerHeatMapItem"
    post:
      operationId: getVolunteerHeatMap
      description: Тепловая карта отчетов для администраторов
      tags:
        - report-heat-map
      parameters:
        - name: searchQuery
          in: query
          required: true
          schema:
            type: string
        - name: pageRequest
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/PageRequest"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportsHeatMapFilter"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportsHeatMapPageResponse"
  /reports/heat-map/currentUser:
    get:
      operationId: getCurrentUserHeatMap
      description: Тепловая карта отчетов текущего пользователя
      tags:
        - report-heat-map
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                description: Тепловые карты пользователя за текущий и прошлый год
                additionalProperties:
                  $ref: "#/components/schemas/VolunteerHeatMapItem"
  /application/create:
    post:
      operationId: createApplication
      tags:
        - application
      parameters:
        - name: captchaToken
          in: query
          required: true
          schema:
            type: string
            nullable: false
            description: Turnstile Captcha Token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationDto"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationStatusDto"
  /application/{id}:
    get:
      operationId: getApplication
      tags:
        - application
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDto"
  /application/{id}/note:
    post:
      operationId: addNoteToApplication
      description: Добавление заметки по заявке
      tags:
        - application
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NoteDto"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteDto"
  /application/update:
    put:
      operationId: updateApplication
      tags:
        - application
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationDto"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDto"
  /applications:
    post:
      operationId: getApplications
      tags:
        - application
      parameters:
        - name: pageRequest
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/PageRequest"
        - name: searchQuery
          in: query
          required: false
          schema:
            type: string
      requestBody:
        required: false
        content:
          "*/*":
            schema:
              $ref: "#/components/schemas/ApplicationsFilter"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationPageResponse"
  /application/status/{id}:
    get:
      operationId: getApplicationStatus
      tags:
        - application
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
            nullable: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationStatusDto"
  /application/searchByEmail:
    get:
      operationId: searchApplicationByEmail
      tags:
        - application
      parameters:
        - name: email
          in: query
          required: true
          schema:
            type: string
            nullable: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationStatusDto"

  /mail/send:
    post:
      operationId: sendMail
      tags:
        - mail
      parameters:
        - name: subject
          in: query
          required: true
          schema:
            type: string
            minLength: 3
        - name: from
          in: query
          required: false
          schema:
            type: string
        - name: email
          in: query
          required: false
          schema:
            type: string
        - name: username
          in: query
          required: false
          schema:
            type: string
        - name: attachments
          in: query
          style: form
          required: false
          schema:
            type: array
            items:
              type: string
              nullable: false
      requestBody:
        required: true
        content:
          text/plain:
            schema:
              type: string
      responses:
        "200":
          description: Success

  /note/{id}:
    delete:
      operationId: deleteNote
      tags:
        - note
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Success

  /programs:
    get:
      operationId: getPrograms
      tags:
        - programs
      summary: Получить список всех доступных программ
      responses:
        "200":
          description: Список программ
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProgramDto"
        "500":
          $ref: "#/components/responses/ServerError"

  /projects:
    get:
      operationId: getProjects
      tags:
        - projects
      summary: Получить список всех доступных проектов
      responses:
        "200":
          description: Список проектов
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectDto"
        "500":
          $ref: "#/components/responses/ServerError"

  /official-group:
    get:
      operationId: getOfficialGroup
      tags:
        - officialGroup
      summary: Получить список всех доступных официальных групп
      responses:
        "200":
          description: Список официальных групп
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfficialGroupDto"
        "500":
          $ref: "#/components/responses/ServerError"

  /cities:
    get:
      operationId: getCities
      tags:
        - cities
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CityDto"
        "500":
          $ref: "#/components/responses/ServerError"
  /cities/{code}:
    get:
      operationId: findCityByCode
      tags:
        - cities
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
          description: City code (e.g., "belgrade", "novi-sad")
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CityDto"
        "404":
          description: City not found
        "500":
          $ref: "#/components/responses/ServerError"
  /cities/search:
    get:
      operationId: findCityByName
      tags:
        - cities
      parameters:
        - name: name
          in: query
          required: true
          schema:
            type: string
          description: City name to search (supports both Latin and Cyrillic, e.g., "Beograd", "Белград")
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CityDto"
        "500":
          $ref: "#/components/responses/ServerError"

  /statistics/{year}:
    get:
      operationId: getStatistics
      tags:
        - statistics
      parameters:
        - name: year
          in: path
          description: Год для получения статистики для мин. труд
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Статистика для мин. труд по группам за год
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Statistics"
        "500":
          $ref: "#/components/responses/ServerError"

  /ticket:
    post:
      operationId: createTicket
      description: Создание тикета (обращения)
      tags:
        - tickets
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TicketCreateRequest"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketCreateResponse"
  /ticket/groups:
    get:
      operationId: getTicketGroups
      description: Получить доступные группы поддержки
      tags:
        - tickets
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: array
                default: [ ]
                items:
                  type: string
                  default: Название группы поддержки для создания тикета

  /csrf:
    get:
      operationId: getCsrfToken
      summary: Get CSRF token
      description: Triggers CSRF token cookie to be set for SPA frontend
      tags:
        - security
      responses:
        "200":
          description: CSRF token cookie set successfully

  /translate/serbian:
    post:
      operationId: translateToSerbian
      tags:
        - translate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TextTranslationRequest"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextTranslationResponse"

components:
  responses:
    ServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    VolunteerStatistics:
      description: Информация о волонтерах
      type: object
      properties:
        femaleCount:
          type: integer
        maleCount:
          type: integer
        age15to18Count:
          type: integer
        age18to30Count:
          type: integer
        age30to40Count:
          type: integer
        age40to65Count:
          type: integer
        age65AndAboveCount:
          type: integer
        citizensCount:
          type: integer
        foreignersCount:
          type: integer
    StatisticData:
      type: object
      properties:
        count:
          type: integer
          nullable: true
          description: количество волонтеров в группе
        totalTimeSpent:
          type: number
          format: double
          nullable: true
          description: общее время, затраченное в часах всеми волонтерами в группе
    ProgramStatItem:
      type: object
      required: [ code, data ]
      properties:
        code:
          type: string
          nullable: true
          description: Код статистической программы; null = «прочее»
        data:
          $ref: "#/components/schemas/StatisticData"
    ProgramStatistics:
      type: object
      required: [ items, total ]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ProgramStatItem"
        total:
          $ref: "#/components/schemas/StatisticData"
      description: Универсальная сводка по программам
    FinalUsersStatistics:
      description: Данные о конечных пользователях волонтерской деятельности
      type: object
      properties:
        culturalAssetsCount:
          type: integer
        naturalAssetsCount:
          type: integer
        publicAreasCount:
          type: integer
        otherCount:
          type: integer
        totalCount:
          type: integer
    Statistics:
      type: object
      properties:
        year:
          type: integer
          description: Отчетный год
        programStatistics:
          $ref: "#/components/schemas/ProgramStatistics"
        volunteerStatistics:
          $ref: "#/components/schemas/VolunteerStatistics"
        finalUsersStatistics:
          $ref: "#/components/schemas/FinalUsersStatistics"
    PageRequest:
      type: object
      description: Paging object
      properties:
        pageNumber:
          type: integer
          format: int32
          default: 0
          nullable: false
        pageSize:
          type: integer
          format: int32
          default: 10
          nullable: false
        sort:
          type: array
          default: [ ]
          items:
            type: string
    PageResponse:
      type: object
      nullable: false
      description: Page info
      required:
        - page
        - size
        - totalPages
        - totalElements
      properties:
        pageNumber:
          type: integer
          format: int32
        pageSize:
          type: integer
          format: int32
        totalPages:
          type: integer
          format: int32
        totalElements:
          type: integer
          format: int64
    ErrorResponse:
      type: object
      description: Общий объект для сообщений об ошибках
      required:
        - message
      properties:
        message:
          type: string
          description: Описание ошибки
          nullable: false
    TextTranslationRequest:
      type: object
      required:
        - text
      properties:
        text:
          type: string
          nullable: false
          description: Source text to translate into Serbian
    TextTranslationResponse:
      type: object
      required:
        - text
      properties:
        text:
          type: string
          nullable: false
          description: Translated Serbian text
    GenderEnumDto:
      type: string
      enum:
        - MALE
        - FEMALE
    UserCreateRequest:
      type: object
      required:
        - email
        - username
        - fullName
        - contract
      properties:
        email:
          type: string
          format: email
          maxLength: 100
        username:
          type: string
          minLength: 2
          maxLength: 100
        fullName:
          type: string
          minLength: 2
          maxLength: 100
        contract:
          $ref: "#/components/schemas/ContractDto"
    UserInfoDto:
      type: object
      description: Дополнительная информация о пользователе
      required:
        - id
        - username
        - email
        - fullName
        - groups
        - active
      properties:
        id:
          type: integer
          nullable: false
        username:
          type: string
          nullable: false
        email:
          type: string
          nullable: false
        fullName:
          type: string
          nullable: false
        city:
          type: string
        postalCode:
          type: string
        address:
          type: string
        birthDate:
          type: string
          format: date
        telegram:
          type: string
        phone:
          type: string
        program:
          $ref: "#/components/schemas/ProgramDto"
        project:
          $ref: "#/components/schemas/ProjectDto"
        avatar:
          $ref: "#/components/schemas/FileInfoDto"
        groups:
          type: array
          uniqueItems: true
          items:
            type: string
        active:
          type: boolean
          nullable: false
        gender:
          $ref: "#/components/schemas/GenderEnumDto"
        contracts:
          type: array
          default: [ ]
          items:
            $ref: "#/components/schemas/ContractDto"
        residencePermits:
          type: array
          default: [ ]
          items:
            $ref: "#/components/schemas/ResidencePermitDto"
    UserInfoUpdateRequest:
      type: object
      description: обновить информацию о пользователе
      properties:
        city:
          type: string
        postalCode:
          type: string
        address:
          type: string
        birthDate:
          type: string
          format: date
        telegram:
          type: string
        phone:
          type: string
        gender:
          $ref: "#/components/schemas/GenderEnumDto"
      additionalProperties: false
    UserPageResponse:
      type: object
      nullable: false
      required:
        - page
        - content
      properties:
        page:
          $ref: "#/components/schemas/PageResponse"
        content:
          type: array
          default: [ ]
          nullable: false
          items:
            $ref: "#/components/schemas/UserInfoDto"
    FileInfoDto:
      type: object
      nullable: true
      required:
        - id
        - name
        - link
        - size
      properties:
        id:
          type: string
          nullable: false
        name:
          type: string
          nullable: false
        link:
          type: string
          nullable: false
        contentLength:
          type: integer
          format: int64
          nullable: false
          description: Size (bytes)
    ReportFilter:
      type: object
      properties:
        login:
          type: string
          nullable: true
        dateFrom:
          type: string
          format: date
          nullable: true
        dateTo:
          type: string
          format: date
          nullable: true
        status:
          type: string
          nullable: true
        program:
          type: string
          nullable: true
          description: Показывать только отчеты по указанной программе. Фильтр работает по программе, указанной в самом отчете (на момент создания отчета), а не по текущей программе волонтера. Пустая строка значит показать отчеты без заполненной программы.
        project:
          type: string
          nullable: true
          description: Показывать только отчеты по указанному проекту. Фильтр работает по проекту, указанному в самом отчете (на момент создания отчета), а не по текущему проекту волонтера. Пустая строка значит показать отчеты без заполненного проекта.
    ReportDto:
      type: object
      nullable: false
      description: Report information
      required:
        - id
        - tasks
      properties:
        id:
          type: string
          format: uuid
        createTime:
          type: string
          format: date-time
          description: Время создания
        user:
          type: string
          description: Логин пользователя
        status:
          type: string
          description: Статус отчета
        week:
          type: number
          description: Номер отчетной недели
        program:
          type: string
          description: Код программы
          example: IT
        project:
          type: string
          description: Код проекта
          example: PORTAL
        isAuto:
          type: boolean
          nullable: false
          description: Автоматически сгенерированный отчет
          example: false
        tasks:
          type: array
          default: [ ]
          description: Список задач в отчете
          items:
            $ref: "#/components/schemas/TaskDto"
        notes:
          type: array
          default: [ ]
          description: Список заметок от модератора
          items:
            $ref: "#/components/schemas/NoteDto"
    TaskDto:
      type: object
      description: Task information
      required:
        - id
        - date
        - name
        - description
        - timeSpent
      properties:
        id:
          type: string
          format: uuid
        date:
          type: string
          format: date
          description: Когда задача была выполнена
        name:
          type: string
        description:
          type: string
        nameSr:
          type: string
          nullable: true
        descriptionSr:
            type: string
            nullable: true
        customer:
          type: string
          description: Login of customer
          nullable: true
        timeSpent:
          type: integer
          description: Time spent in minutes
        result:
          type: string
          nullable: true
        files:
          type: array
          default: [ ]
          items:
            $ref: "#/components/schemas/FileInfoDto"
    ReportPageResponse:
      type: object
      nullable: false
      required:
        - page
        - content
      properties:
        page:
          $ref: "#/components/schemas/PageResponse"
        content:
          type: array
          default: [ ]
          nullable: false
          items:
            $ref: "#/components/schemas/ReportDto"
    ContractDto:
      type: object
      required:
        - id
        - startDate
        - endDate
        - type
      properties:
        id:
          type: string
          format: uuid
          nullable: false
        startDate:
          type: string
          format: date
          nullable: false
        endDate:
          type: string
          format: date
          nullable: false
        type:
          $ref: "#/components/schemas/ContractTypeEnum"
    ResidencePermitDto:
      type: object
      required:
        - id
        - nationality
        - registrationNumber
        - validUntil
        - purposeOfStay
        - issuingAuthority
        - stateOfBirth
        - issuingDate
        - identityNumber
        - frontSidePhoto
        - backSidePhoto
      properties:
        id:
          type: string
          format: uuid
          nullable: false
        nationality:
          type: string
          nullable: false
        registrationNumber:
          type: string
          nullable: false
        validUntil:
          type: string
          format: date
          nullable: false
        purposeOfStay:
          type: string
          nullable: false
        note:
          type: string
          nullable: true
        issuingAuthority:
          type: string
          nullable: false
        stateOfBirth:
          type: string
          nullable: false
        issuingDate:
          type: string
          format: date
          nullable: false
        identityNumber:
          type: string
          nullable: false
        frontSidePhoto:
          $ref: "#/components/schemas/FileInfoDto"
        backSidePhoto:
          $ref: "#/components/schemas/FileInfoDto"
    ApplicationDto:
      type: object
      required:
        - id
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          maxLength: 50
        type:
          type: string
          maxLength: 50
        created:
          type: string
          format: date-time
        email:
          type: string
          format: email
          maxLength: 100
        name:
          type: string
          minLength: 1
          maxLength: 100
        patronymic:
          type: string
          maxLength: 100
        birthDate:
          type: string
          format: date
        passport:
          type: string
          maxLength: 100
        citizenship:
          type: string
          maxLength: 100
        telegram:
          type: string
          maxLength: 100
        inSerbia:
          type: boolean
        enterDate:
          type: string
          format: date
        city:
          type: string
          maxLength: 100
        postalCode:
          type: string
          maxLength: 20
        address:
          type: string
          maxLength: 200
        phone:
          type: string
          maxLength: 30
        residenceRequired:
          type: boolean
        occupation:
          type: string
          maxLength: 100
        gender:
          $ref: "#/components/schemas/GenderEnumDto"
        hasExperience:
          type: boolean
        experience:
          type: string
          maxLength: 1000
        languages:
          type: string
          maxLength: 200
        skills:
          type: string
          maxLength: 1000
        goal:
          type: string
          maxLength: 2000
        bio:
          type: string
          maxLength: 1000
        refuseReason:
          type: string
          maxLength: 2000
        comment:
          type: string
          maxLength: 2000
        contract:
          $ref: "#/components/schemas/ContractDto"
        program:
          type: string
          description: Код программы
          example: IT
        project:
          type: string
          description: Код проекта
          example: PORTAL
        notes:
          type: array
          default: [ ]
          items:
            $ref: "#/components/schemas/NoteDto"
    ApplicationsFilter:
      type: object
      required:
        - showCompleted
      properties:
        showCompleted:
          type: boolean
          default: false
          description: Показывать обработанные заявки
    ApplicationStatusDto:
      type: object
      required:
        - id
        - status
        - progress
        - terminated
        - lastUpdate
      properties:
        id:
          type: string
          format: uuid
          nullable: false
        status:
          type: string
          nullable: false
        progress:
          type: integer
        terminated:
          type: boolean
        refuseReason:
          type: string
        lastUpdate:
          type: string
          format: date
          nullable: false
    ApplicationPageResponse:
      type: object
      nullable: false
      required:
        - page
        - content
      properties:
        page:
          $ref: "#/components/schemas/PageResponse"
        content:
          type: array
          default: [ ]
          nullable: false
          items:
            $ref: "#/components/schemas/ApplicationDto"
    NoteDto:
      type: object
      nullable: false
      required:
        - id
        - text
      properties:
        id:
          type: string
          format: uuid
        createTime:
          type: string
          format: date-time
        createdBy:
          type: string
          description: Логин пользователя, который создал заметку
          maxLength: 100
        text:
          type: string
          minLength: 1
          maxLength: 5000
    ChangeReportStatusRequest:
      type: object
      nullable: false
      required:
        - status
      properties:
        status:
          type: string
          nullable: false
        note:
          type: string
    UserSearchFilter:
      type: object
      nullable: false
      properties:
        onlyActive:
          type: boolean
          nullable: true
          description: Показывать только активных пользователей
        onlyInactive:
          type: boolean
          nullable: true
          description: Показывать только неактивных пользователей (onlyActive приоритетней если указаны оба)
        program:
          type: string
          nullable: true
          description: Показывать только пользователей по указанной программе, пустая строка значит показать пользователей без программы
          example: "IT"
        project:
          type: string
          nullable: true
          description: Показывать только пользователей по указанному проекту, пустая строка значит показать пользователей без проекта
          example: "DESIGN_3D"
    ContractTypeEnum:
      type: string
      enum:
        - ASSOCIATED
        - REGULAR
    CityDto:
      type: object
      required:
        - code
        - name
        - nameCyrillic
        - hasMup
      properties:
        code:
          type: string
          description: City code (lowercase latin name)
        name:
          type: string
          description: City name in Serbian Latin
        nameCyrillic:
          type: string
          description: City name in Cyrillic
        hasMup:
          type: boolean
          description: Whether the city has a MUP office
    ProgramDto:
      type: object
      required:
        - code
        - nameRu
        - nameEn
        - nameSr
      properties:
        code:
          type: string
        nameRu:
          type: string
        nameEn:
          type: string
        nameSr:
          type: string
        projectCodes:
          type: array
          description: List of project codes related to this program
          items: { type: string }
        officialGroups:
          type: array
          description: List of official group codes related to this program
          items: { type: string }
    ProjectDto:
      type: object
      required:
        - code
        - nameRu
        - nameEn
        - nameSr
      properties:
        code:
          type: string
        nameRu:
          type: string
        nameEn:
          type: string
        nameSr:
          type: string
        programCode:
          type: string
          description: Owning program code
    OfficialGroupDto:
      type: object
      required:
        - code
        - nameRu
        - nameEn
        - nameSr
      properties:
        code:
          type: string
        nameRu:
          type: string
        nameEn:
          type: string
        nameSr:
          type: string
    ReportsHeatMapPageResponse:
      type: object
      required:
        - content
        - page
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/VolunteerHeatMapItem"
        page:
          $ref: "#/components/schemas/PageResponse"
    VolunteerHeatMapItem:
      type: object
      required:
        - volunteerInfo
        - weeks
      properties:
        volunteerInfo:
          $ref: "#/components/schemas/UserInfoDto"
        weeks:
          type: array
          description: Информация по каждой неделе
          items:
            $ref: "#/components/schemas/HeatMapItem"
        totalWorked:
          type: number
          description: Кол-во засчитанных часов по отчетам по всем неделям
          nullable: false
        totalRequired:
          type: number
          description: Кол-во требуемых часов суммарно по всем неделям
          nullable: false
    HeatMapItem:
      type: object
      required:
        - week
        - weekStart
        - weekEnd
        - hoursWorked
        - hoursRequired
      properties:
        week:
          type: integer
          description: Номер недели
          nullable: false
          example: 52
        weekStart:
          type: string
          format: date
          description: Дата начала недели
          nullable: false
        weekEnd:
          type: string
          format: date
          description: Дата окончания недели
          nullable: false
        hoursWorked:
          type: number
          description: Кол-во засчитанных часов по отчетам в указанную неделю
          nullable: false
        hoursRequired:
          type: number
          description: Кол-во требуемых часов в указанную неделю
          nullable: false
    ReportsHeatMapFilter:
      type: object
      properties:
        year:
          type: integer
          nullable: true
          description: Год, по которому составить тепловую карту, по умолчанию текущий
          example: 2026
        program:
          type: string
          nullable: true
          description: Показывать только пользователей по указанной программе, пустая строка значит показать пользователей без программы
          example: "IT"
        project:
          type: string
          nullable: true
          description: Показывать только пользователей по указанному проекту, пустая строка значит показать пользователей без проекта
          example: "DESIGN_3D"
    TicketCreateRequest:
      type: object
      required:
        - fromUser
        - toUser
        - group
        - title
        - body
      properties:
        fromUser:
          type: string
          description: Логин пользователя, который создал тикет (или null если тикет от системы)
          nullable: true
        toUser:
          type: string
          description: Логин пользователя, на которого заведен тикет (например куратор завел на волонтера)
          nullable: true
        group:
          type: string
          description: Имя группы, на которую будет назначен тикет
          nullable: false
        title:
          type: string
          description: Загловок (название) обращения
          nullable: false
        body:
          type: string
          description: Текст обращения
          nullable: false
        attachments:
          type: array
          description: Прилагаемые файлы
          items:
            type: string
            description: Идентификатор файла
    TicketCreateResponse:
      type: object
      properties:
        id:
          type: integer
          description: Идентификатор созданного тикета
        ticketLink:
          type: string
          description: Ссылка на тикет в системе HelpDesk
